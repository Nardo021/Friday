# Friday Roadmap

## v1 — Cursor Agent Portal (current)

- Monorepo structure
- Multi-session manager
- `cursor-cli-local` adapter + CursorCliLauncher + PTY
- `external-cursor-observer` + process discovery
- Agent Control Panel
- Safe session close
- Terminal resize API
- SQLite schema v2
- SDK/Cloud stubs
- **Pet Engine** — moving native window, patrol, status bubble, click-through

## v1.1 — Pet Polish

- Spritesheet / pixel-accurate alpha hit-test
- Multi-monitor spawn on active display
- Jump-to-attention on approval

## v2 — Cursor Cloud Agent + Immersive Pet (experimental)

- `@friday/sdk-worker` Node process + IPC
- `cursor-cloud-agent` adapter live
- API key onboarding (keyring)
- PR / artifact events in Panel
- Cloud session history
- **Optional fullscreen overlay pet mode** (disabled by default; advanced click-through)

## v3 — Mobile Remote

- iOS companion (observe + approve + stop)
- Push notifications for approvals
- Local HTTP bridge from desktop

## v4 — Usage Dashboard

- Session duration analytics
- Repo activity heatmap
- Agent success/failure rates
- Weekly productivity report in Command Center
